<controls:MetroWindow x:Class="RxTracking.Views.OrderView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:ignore="http://www.galasoft.ch/ignore"
        mc:Ignorable="d ignore"
        DataContext="{Binding OrderVM, Source={StaticResource Locator}}"
        Title="{Binding Title}">

    <Grid>
        <Label x:Name="label" Content="FullName" HorizontalAlignment="Left" Margin="267,226,0,0" VerticalAlignment="Top" Width="62" Height="33"/>
        <TextBlock x:Name="textBlock" HorizontalAlignment="Left" Margin="356,231,0,0" TextWrapping="Wrap" Text="{Binding FullName}" VerticalAlignment="Top" 
                   Height="28" Width="72"/>
    </Grid>
</controls:MetroWindow>
